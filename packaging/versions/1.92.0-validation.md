# Boost 1.92.0 validation

Current target status:

- official source SHA-256 verification: enforced by the builder;
- universal archive validation: pending a clean local build;
- arm64 and x86_64 symbol validation: pending a clean local build;
- standalone XCFramework runtime validation: pending;
- SwiftPM consumer validation: pending;
- local CocoaPods consumer validation: pending;
- Homebrew audit, clean install, and smoke test: pending;
- final artifact checksum verification: pending.

Only checks actually observed locally or proven by CI should be changed to
passed.
